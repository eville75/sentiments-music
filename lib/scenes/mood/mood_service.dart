class MoodService {
  // Exemplo futuro de integração (salvar histórico de sentimentos, analytics, etc)
  Future<void> registerMood(String mood) async {
    await Future.delayed(const Duration(milliseconds: 200));
    return;
  }
}
